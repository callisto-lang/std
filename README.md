# Callisto standard library

## How to use
Add this repository as a submodule in your project
```
git submodule add https://github.com/callisto-lang/std
```

Then add `-i std` to the compiler flags when building
