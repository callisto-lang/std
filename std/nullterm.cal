## ## printntstr
## Parameters: addr str
##
## Prints a null-terminated string to stdout from the given pointer
func printntstr begin
	let addr str
	str !

	while str @ c@ do
		str @ c@ printch
		str @ 1 + str !
	end
end
